#include <iostream>
#include <string>
#include <vector>
using namespace std;
int main(){
    string text, word;
    vector<string> texts;
    cin >> word;
    int cnt = 0;
    while(cin >> text){
        if (text == word){
            cnt++;
    } 
    
    return 0;
}