#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    int x, y;
    cin >> x >> y;
    char a[x][y];
    for (int i = 0; i < x; i++){
        for (int j = 0; j < y; j++){
            cin >> a[i][j];
        }
    }
    int cnt = 0;
    for (int i = 0; i < x; i++){
        for (int j = 0; j < y; j++){
            if ('.'==a[i][j]){
                
            }
        }
    }

    return 0;
}